<nav class="navbar navbar-expand-sm navbar-light bg-light mb-0 fixed-top">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="container d-flex justify-content-between navbar-nav mr-auto">
      <%= link_to root_path, class: "navbar-brand" do %> <%= image_tag "logoWagon Horti.png", class: "nav-img" %> <% end %>
      <% if user_signed_in? %>
        <div class="container d-flex justify-content-end nav-item">
          <li class="nav-item m-2"> <%= current_user.first_name %> </li>
          <li class="nav-item dropdown">
            <%= image_tag "https://kitt.lewagon.com/placeholder/users/TonyDantas", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <%= link_to "Sair", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
        </div>
      <% else %>
        <li class="container d-flex justify-content-end nav-item">
          <%= link_to "Entrar", user_session_path, class: "nav-link" %>
          <%= link_to "Registre-se", new_user_registration_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
